{
  "name": "game-template",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "dev": "bun run list && bun run server",
    "upload": "bun run server/upload-files.ts"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.116.0"
  }
}